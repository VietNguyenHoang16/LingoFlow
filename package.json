{
  "name": "lingoflow",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "bash scripts/vercel-build.sh",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
